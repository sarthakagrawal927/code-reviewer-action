# API Worker (Planned)

This directory is reserved for the hosted API worker in future versions.

Planned responsibilities:

- GitHub App webhook ingestion
- Repo/workspace configuration API
- Review orchestration endpoints
- Signed request validation for hosted action mode
